{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "type": "object",
  "properties": {
    "id": {
      "type": "string",
      "description": "Version identifier for the skill. Each version is identified by a Unix epoch timestamp (e.g., \"1759178010641129\")."
    },
    "type": {
      "type": "string",
      "enum": ["skill_version_deleted"],
      "description": "Deleted object type. For Skill Versions, this is always 'skill_version_deleted'.",
      "default": "skill_version_deleted"
    }
  },
  "required": ["id", "type"]
}
